# SPEC-MOBILE-003 Progress

- Started: 2026-06-11 17:53
- Execution mode: sub-agent (user-selected), current branch (feature/SPEC-MOBILE-001)
- Development mode: tdd (quality.yaml)
- Harness level: standard (evaluator final-pass; auth surfaces are preserved MOBILE-002 assets — escalate to thorough on CRITICAL)
- Phase 0.5 complete: memory_guard=disabled, skipped
- Phase 0.9 complete: detected_language_skills=[moai-lang-typescript]
- Phase 0.95 complete: Scale-based mode: Full Pipeline (files: 20+, domains: 2 — web frontend + mobile RN), sub-agent sequential
- UltraThink: activated for strategy phase (2 domains, 8+ files, user keyword)
- Lessons loading: lessons.md absent, skipped
- Phase 1 complete: manager-strategy execution plan approved (Decision Point 1). T-001~T-010, web-first, OD-1~4 all recommended options. CRITICAL finding: real vitest baseline = 94/94 (SPEC docs say 89/89, stale — fix at sync)
- Phase 1.5 complete: tasks.md generated (10 tasks)
- Phase 1.6 complete: 8 acceptance criteria registered as pending tasks
- Phase 1.7 SKIPPED: file-based routing — file existence IS behavior; empty stubs break next build. Files created within TDD cycles instead
- Phase 1.8 complete: MX context map empty (no existing @MX tags in modify targets) — tags to be ADDED per plan
- Phase 2 start: parallel tracks — Track A (T-001~004 web+destination), Track B (T-005~007 pure core TDD); then sequential T-008~010. Worktree isolation NOT used: required uncommitted/untracked MOBILE-001/002 files (apps/mobile/lib, hooks, components) do not exist in HEAD — a worktree would lack them
- Track A complete (T-001~004): web (main) group + BottomTabBar + HomeTab + placeholders + Figma tokens + shell-mode detection (html[data-shell="native"] + WebViewShell marker prepend) + /me→/home switch. Gates: web tsc 0, next build OK, eslint 0, targeted vitest 16/16. Divergence: +HomeTab.tsx, +_components/PlaceholderTab.tsx (in-scope supporting files)
- Track B complete (T-005~007): route-map-core (17 tests), auth-state-core (10), crossroute dispatch (10), native-back (+3). Full mobile suite 134/134 (baseline 94 + 40 new). tsc 0. decideWebViewLoad widened via TS overloads (legacy callers unchanged). MX: 4 ANCHOR (route-map), 1 ANCHOR + NOTE (auth-state). Drift: 0 unplanned files beyond in-scope supports
- Baseline note: drift guard — planned 10 tasks, T-001~007 actual files match tasks.md + 2 in-scope supporting components (drift < 20%, informational)
- Track C complete (T-008~010): expo-router ~56.2.10 installed (lockfile patch integrity intact: expo-modules-jsi@56.0.7/expo@56.0.8/RN 0.85.3 unchanged), custom entry (env guard first → expo-router/entry last), app/ tree complete ((auth)+(tabs)+index/+not-found), App.tsx removed, BridgedWebView shared seam, AuthContext (@MX:ANCHOR), declarative Redirect guards (Protected fallback documented). Gates: vitest 134/134, tsc 0 (mobile+web), next build OK, expo export OK, AC-8 greps all PASS
- Track C divergences: @expo/vector-icons absent in hoisted layout → emoji glyph tab icons (no new dep); typedRoutes left disabled (R-RT6 Optional — tsconfig lacks .expo/types so it would not enforce; hrefs cast); +BridgedWebView.tsx/TabWebView in-scope supports. Cumulative drift < 20% (informational)
- AC status: AC-2/3/4/8 automated PASS. AC-1/5b/6/7 + R-PR2 OAuth roundtrip = device-gated pending (mobile-spec-device-gated policy)
- Phase 2.75 (pre-review gate): covered by track gates (tsc/eslint/build) — PASS
- Device verification round 1 (2026-06-12, iOS simulator only per user directive — Android excluded, AC-6 Android back 보류):
  - PASS: cold-start entry chain (env guard → expo-router/entry, bundle 1320 modules), splash → (auth)/login WebView render, in-WebView email login (R-PR2), desktop web AC-5a (signup→/home, HomeTab, tab routing), shell-mode hiding mechanism at web layer (after CSP nonce fix)
  - FIXED during verification: CSP-nonce defect — (main)/layout.tsx inline shell-detect script blocked by MOBILE-002 CSP ('nonce-...' + 'strict-dynamic' ignores 'unsafe-inline'); fixed via x-nonce header → script nonce attr; browser-reverified. Android @react-native-cookies jcenter()→mavenCentral() pnpm patch added (build blocker, kept for future)
  - **FAIL D-V1 (CRITICAL)**: Next.js App Router soft navigation (server-action redirect, <Link> pushState) does NOT trigger onShouldStartLoadWithRequest → navigation contract (cross-route deny+dispatch, R-NC2/NC3) never fires for in-app web navigation. Post-login the login WebView soft-navigated to /home INSIDE itself; native (tabs) never mounted. Evidence: post-login tab bar = WEB BottomTabBar (top indicator + lucide icons), not native emoji-glyph Tabs. AC-1/AC-3 runtime FAIL
  - **FAIL D-V2 (HIGH)**: no web→native token handover on fresh in-WebView login — web only replies session:synced to native session:restore (requires pre-existing tokens = chicken-and-egg). SecureStore never seeded → cold restart lands on login. AC-4 session persistence FAIL. Latent SPEC-MOBILE-002 gap (device verification was never run), surfaced now that native routing depends on token cache
  - **FAIL D-V3 (MEDIUM)**: shell-mode tab bar hiding fails on soft navigation into (main) — React-rendered inline script does not execute on client-side route swap; data-shell only set on full document load. Corollary of D-V1
  - Proposed fixes: (F1) shell-mode web→native login announcement — NativeBridgeProvider posts session:synced(+tokens, nonce) on supabase onAuthStateChange SIGNED_IN [fixes D-V2, and login transition becomes signal-driven making D-V1 bypass moot for auth flow]; (F2) shell detection as client-component effect (soft-nav safe) + keep inline script for flash-free initial load [fixes D-V3]; (F3) re-verify navigation contract on tab WebViews (document loads do intercept — desktop initScript simulation passed)
- Fix cycle (user-approved) + device verification rounds 2-3 (2026-06-12, iOS simulator):
  - F2 (ShellModeEffect, soft-nav-safe shell detection): round 2 VERIFIED — web tab bar hidden after soft navigation into (main)
  - F1 (onAuthStateChange announcer): implemented but did NOT fire on device — email login is a SERVER ACTION (cookie session); browser supabase client never emits SIGNED_IN. Root-cause refined
  - F1' (ShellSessionAnnouncer): (main)-mount announcer — shell mode + getSession() cookie read → session:synced(+tokens, nonce, v1 protocol, dedupe by access_token). Round 3 VERIFIED end-to-end on fresh install: server-action email login → (main)/home → synced → SecureStore seeded → AuthContext → native (tabs) mounted (emoji-glyph tab bar + badge). **AC-1 PASS (device)**
  - Cold restart: relaunch lands signed-in on native home without login. **AC-4 session persistence PASS (device)**. D-V2 CLOSED
  - Native tab switch (탐색): native Tabs transition + explore WebView placeholder + web tab bar hidden. D-V1 auth-flow path CLOSED (signal-driven); D-V3 CLOSED
  - Deep link: moyura://auth-callback?code=x via simctl openurl — no route capture, no crash, app stays on home. **AC-7 coexistence PASS (device)**
  - Remaining UNVERIFIED (recorded, not blocking per scope decisions): Google OAuth roundtrip on device (needs real account — user-manual per existing policy), logout E2E (web /me logout UI not reachable from tab flow in this SPEC), AC-3 cross-route dispatch runtime (no cross-route links exist on in-scope pages; covered by vitest contract + desktop simulation), AC-6 Android hardware back runtime (Android excluded by user directive 2026-06-12 — iOS simulator only)
  - Quality gates after fixes: web tsc 0 / eslint 0 / next build OK / mobile vitest 134/134 (no mobile edits in fix cycle)
