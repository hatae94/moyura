# SPEC Review Report: SPEC-SAFETY-001
Iteration: 1/3
Verdict: FAIL
Overall Score: 0.72

Reasoning context ignored per M1 Context Isolation. (No author reasoning was passed; audit performed solely on `.moai/specs/SPEC-SAFETY-001/` artifacts: spec.md primary; plan.md, acceptance.md, spec-compact.md, interview.md, research.md supporting.)

## Must-Pass Results

- [PASS] MP-1 REQ number consistency: Module-scoped sequential numbering, zero gaps, zero duplicates, consistent 3-digit zero-padding. Verified end-to-end: REQ-RPT-001..005 (spec.md:L59–L72), REQ-BLK-001..005 (L76–L89), REQ-FLT-001..006 (L93–L109), REQ-CPL-001..004 (L113–L123), REQ-STO-001..002 (L127–L131). Cross-checked against plan.md §2 (L29–L62) and spec-compact.md (L6–L35): identical 22-REQ set, no drift.
- [PASS] MP-2 EARS format compliance: All 22 requirements carry explicit EARS type tags and structurally match the tagged pattern. Samples: REQ-RPT-001 [Event-driven] "**When** 사용자가 특정 UGC 항목을 사유와 함께 신고하면, 시스템은 … 저장한다(shall)" (spec.md:L60); REQ-FLT-001 [State-driven] "**While** 멤버 B가 사용자 A에게 차단된 동안, 시스템은 … 제외하고 … 드롭한다(shall)" (L94); REQ-RPT-004 [Unwanted] "**If** … **then** 시스템은 신고를 거부한다(400)" (L69); REQ-CPL-001 [Ubiquitous] (L114). Interpretation note (per repo template convention, precedent SPEC-MOIM-002-review-1): acceptance.md is the GWT artifact ("Given/When/Then 시나리오", acceptance.md:L5) and spec.md §5 is the EARS artifact — GWT ACs are NOT mislabeled EARS. Two non-blocking formality deviations recorded as D5/D6 (missing "(shall)" token on REQ-RPT-004; [Optional]-pattern semantic misuse on REQ-BLK-004) — structurally still pattern-conformant, so MP-2 holds.
- [PASS] MP-3 YAML frontmatter validity: All 8 contracted fields present with correct types (spec.md:L1–L10): `id: SPEC-SAFETY-001` (string, SPEC-{DOMAIN}-{NUM}) L2, `version: "0.1.0"` (string) L3, `status: draft` (valid enum) L4, `created: 2026-07-02` (ISO date) L5, `updated: 2026-07-02` L6, `author: hatae` L7, `priority: critical` (valid lowercase enum) L8, `issue_number: 0` (integer; 0 = no linked issue, consistent with local-only repo) L9.
- [N/A] MP-4 Section 22 language neutrality: N/A — single-stack product SPEC (NestJS backend + Prisma + Supabase + Next.js web, spec.md:L48). Not template-bound or multi-language tooling content. Auto-pass.

## Category Scores (0.0–1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.60 | between 0.50 and 0.75 — multiple points require interpretation | spec.md REQs are mostly single-interpretation with concrete values (400/200/204, block∪report union, '차단한 멤버' label). Pulled down by plan.md-internal contradictions on the REQ-FLT-006 mechanism (D1: L50 `getHiddenUserIds` vs L121/L139 `getBlockersOf`; D2: L135 wrong argument) and by "우선 채팅 메시지 말풍선" (plan.md:L148) leaving report-entry UI scope undetermined (D4). Mask-label semantics for report-only hiding unaddressed (D14). |
| Completeness | 0.75 | 0.75 — one non-critical gap class; frontmatter complete | All sections present: HISTORY (spec.md:L16–L22), Goal (L24), Context (L28), policy mapping (L37), Assumptions (L46), 22 EARS REQs (L53–L131), Delta Markers (L133), Exclusions with 13 specific falsifiable entries (L151–L165), Dependencies (L167), Quality Gate (L173); GWT ACs + edge cases + DoD in acceptance.md. Holes: settlement_request display surface unspecified (D3); poll/expense/settlement report entry points neither planned nor excluded (D4); R-15 referenced but undefined (D10). |
| Testability | 0.85 | between 0.75 and 1.0 | All 17 ACs are binary-testable with concrete Given/When/Then and assertable outcomes (e.g., AC-BLK-1 "P2002 → 200 … 자기 차단은 400" acceptance.md:L36; AC-FLT-6 contrast case "report만 있고 … push 유지" L79–L80; AC-CPL-1 union {B,C} L87). No weasel words ("적절/합리적/appropriate/reasonable") found in any AC. Minor interpretation: AC-STO-1 "검토 가능" + "운영 절차 문서로 대응" (L104) is testable only as document-existence + field-presence; AC-CPL-1 "요청당 1회 조회" needs mock-call-count convention (stated in plan M3, acceptable). |
| Traceability | 0.90 | between 0.75 and 1.0 | Bidirectional REQ↔AC mapping is complete: every one of the 22 REQs carries an "— AC:" line citing an existing AC (spec.md:L60–L131); every AC header cites only existing REQs (acceptance.md:L9, L14, L19, L26, L33, L38, L43, L50, L55, L60, L65, L70, L75, L84, L89, L94, L101). No orphaned ACs, no uncovered REQs. Deductions: AC-CPL-2 verifies a push-import ban absent from REQ-CPL-002 text (D8); acceptance.md:L116 mis-cites R-11 (D9); plan.md cites undefined R-15 (D10). |

## Checklist Summary

- FC-1..FC-6 (frontmatter, per 8-field project contract): PASS — see MP-3.
- SC-1 HISTORY: PASS (spec.md:L16). SC-2 WHY: PASS (L24–L35). SC-3 WHAT: PASS (L53–L131). SC-4 REQUIREMENTS: PASS (22 entries). SC-5 ACCEPTANCE CRITERIA: PASS (acceptance.md, linked at spec.md:L14 per template). SC-6 Exclusions: PASS — 13 concrete entries (L151–L165), each falsifiable (관리자 UI, 글로벌 삭제, 2-way 차단, 복합 PK 신고, dates/window 필터, 브로드캐스트 서버 필터, fan-out 차감, push 리보크, 전용 라우트, 네이티브 변경, 계정 삭제, 신규 의존성).
- RQ-3/RQ-4 (WHAT not HOW): PASS with note — REQ-CPL module is intentionally architectural (module boundary/authz constraints are the requirement in this brownfield convention); REQ-RPT-005 (TEXT 저장 + BigInt 캐스팅, spec.md:L72) is data-representation detail that belongs in plan, tolerated as it pins a cross-cutting migration contract.
- CN-1 (no contradictions): FAIL — D1/D2/D7/D10 (plan.md-internal + spec.md self-claim).
- CN-2 (exclusions vs REQs): PASS with gap — exclusions consistent with REQs; but report entry-point scope is in neither (D4).
- CN-3 (priority/scope consistency): PASS — `priority: critical` matches store-release blocker framing (spec.md:L26).
- Cross-SPEC contract (orchestrator-supplied): RESPECTED — orphan cleanup owned by SPEC-ACCOUNT-001 (spec.md:L164; plan.md:L159–L168), implementation order SAFETY-first (spec.md:L51, L164; plan.md:L165–L167), no-op guard stated (plan.md:L301). Terminology drift noted in D13.
- Interview scope alignment: PASS with provenance gap (D12) — the 4 Round-1 answers (SPEC split, all-UGC hide + dual entry points + independence, ACCOUNT-001 deferral, report store+hide+prompt+manual ops) are all reflected in REQs; the 4 later "확정 결정" claimed in HISTORY (spec.md:L18) have no interview.md record, and expense row-retention+masking deviates from the literal Round-1 answer "모든 UGC 숨김" (interview.md:L16) on the strength of an off-disk approval claim.

## Defects Found

D1. plan.md:L50 — REQ-FLT-006 (plan §2 requirement text) instructs "`push.listener.ts`의 수신자 산정(moim_member − sender)에 **`getHiddenUserIds` 역조회**를 차감한다", but `getHiddenUserIds` is defined as the block∪report union (plan.md:L120, spec.md:L114) while the very same sentence mandates block-only suppression ("오직 명시적 `block`만 push를 억제"). The correct reverse-path method is `getBlockersOf` per plan.md:L121, L139, L232, L262, L269, spec.md:L32, and AC-FLT-6 (acceptance.md:L78). An implementer following plan §2 verbatim would suppress push for reported-but-not-blocked users, directly violating AC-FLT-6's contrast case (acceptance.md:L79–L80). — Severity: major
D2. plan.md:L135 — "`getBlockersOf(recipientUserIds)`를 1회 조회해 발신자를 차단한 수신자를 차감한다" contradicts the method's own definition (plan.md:L121: `where blockedUserId in userIds` → returns blockerIds). Passing `recipientUserIds` returns "users who blocked any recipient", not "recipients who blocked the sender". The correct call is `getBlockersOf([senderId])` as written at plan.md:L139 and L262. Two conflicting statements four lines apart in the same section. — Severity: major
D3. spec.md:L63, L69 vs L93–L109; research.md:L27 — Coverage hole: `settlement_request` is a reportable content type (REQ-RPT-004 whitelist, spec.md:L69; AC-RPT-4, acceptance.md:L29) and a UGC surface rendered in expenses-view.tsx via expense (research.md:L27, `requester_id`), yet no REQ-FLT entry, no AC, and no plan delta specifies how a hidden user's settlement_request is hidden or masked in the viewer's expense view. REQ-RPT-002 promises "해당 콘텐츠(및 동일 작성자 UGC)를 즉시 숨긴다" (spec.md:L63) but REQ-FLT-003 masks only "B가 만든 expense 행" authors (spec.md:L100); the settlement/transactions surface for a hidden requester is unspecified behavior. — Severity: major
D4. plan.md:L148, L213 vs spec.md:L69, §7 (L151–L165) — Report entry-point UI scope is ambiguous: the backend accepts 4 content types (chat_message/poll/expense/settlement_request), but the web plan builds a report entry only on chat message bubbles ("우선 채팅 메시지 말풍선 — chat/page.tsx"), and the entry points for the other 3 types are neither planned (Delta Markers touch only chat/page.tsx for report entry, spec.md:L144) nor listed in Exclusions. "우선" (first/primarily) leaves v1 scope undetermined — an implementer cannot tell whether poll/expense/settlement report buttons are in scope, and the store-policy mapping (spec.md:L41) presumes a reachable report mechanism for UGC. — Severity: major
D5. spec.md:L69 — REQ-RPT-004 is the only requirement lacking the "(shall)" marker used by all 21 other REQs (e.g., L60, L72, L77). Structurally still Unwanted-pattern conformant ("If…, then 시스템은 …거부한다(400)"), so non-blocking. — Severity: minor
D6. spec.md:L86 — REQ-BLK-004 [Optional] uses "**Where** 기존 프로필/설정 화면 내 '차단한 멤버' 섹션에서" as a screen *location*, not a feature-presence conditional. EARS Optional ("Where [feature is included]") is misapplied; the requirement is effectively Ubiquitous ("the system shall provide list+unblock within the profile section"). Type-tag misuse only; the shall-statement itself is testable (AC-BLK-3). — Severity: minor
D7. spec.md:L55 — Self-claim "각 REQ는 단일 행위를 기술하며" is contradicted by compound requirements: REQ-RPT-003 (L66) bundles three behaviors (prompt exposure + accept→block creation + decline→no-block), REQ-BLK-001 (L77) bundles create + idempotency + self-block-400. Cohesive bundles, but the claim as stated is false. — Severity: minor
D8. spec.md:L117 vs acceptance.md:L92, spec.md:L176 — REQ-CPL-002's no-import list names only the 5 production domains (chat/poll/expense/schedule/notification), but AC-CPL-2 and the quality gate additionally verify `push` ("chat/poll/expense/schedule/notification/push … import하지 않음"). The AC tests a constraint stronger than its REQ text. Add push to REQ-CPL-002. — Severity: minor
D9. acceptance.md:L116 — Edge case "계정 삭제 시 고아 행" cites "(R-10, R-11)", but plan.md R-11 (L259) is "복합 PK 콘텐츠 신고 불가" — unrelated to orphan rows. Correct citation is R-10 (and arguably R-15, which itself is undefined — see D10). — Severity: minor
D10. plan.md:L161, L258, L273 — Dangling risk reference: "R-15" (account↔safety 순환 의존 미발생) is cited three times but the §7 risk table (plan.md:L247–L262) defines only R-1..R-14. R-15 is never defined anywhere in the four documents. — Severity: minor
D11. plan.md:L25 — Section header "## 2. EARS 요구사항 설계 (모듈당 ≤5)" is violated by its own module 3, which contains 6 requirements (REQ-FLT-001..006, L45–L50). spec.md:L55 phrases the limit differently ("요구사항 모듈: 5개(모듈 ≤5 한도 준수)" — module *count* ≤5, which holds), so the two documents also disagree on what the limit means. — Severity: minor
D12. spec.md:L18 vs interview.md — Decision provenance gap: HISTORY claims "사용자 확정 결정 4건 반영(지출 행 유지+작성자 마스킹, 프로필 내 '차단한 멤버' 섹션, 고아 정리 소유권…, 탈퇴 사용자 report 행 삭제)", but interview.md records only the original Round-1 4 questions (L10–L22) — none of these 4 decisions appears there, and no other on-disk artifact records the user approval event (plan.md §10 records them as resolutions, not as user answers). Notably, expense row-retention+masking deviates from the literal Round-1 answer "모든 UGC 숨김" (interview.md:L16) solely on this off-disk approval claim. Append the 4 confirmations to interview.md (or an approval log) so the trail is auditable. — Severity: minor
D13. plan.md:L169 vs spec.md:L1–L10; plan.md:L167 vs L301 — plan §3.5 commits "두 SPEC의 frontmatter/plan에 상호 참조 + 구현 순서(SAFETY 테이블 선행)를 명시해 run 단계 순서 혼동을 차단한다", but spec.md's frontmatter carries no dependency/order field (only body §8 does). Also terminology drift on the guard mechanism: §3.5 (L167) prescribes a "후속 위임 태스크" (deferred task) for the ACCOUNT-first case, while §10-3 (L301) calls it an "ACCOUNT 측 no-op 가드" — same intent, different mechanisms (deferred code vs runtime guard); pick one wording. — Severity: minor
D14. spec.md:L100 + plan.md:L125 — Mask-label semantics: the union filter means a reported-but-NOT-blocked author's expense rows are also masked, with the label '차단한 멤버' ("blocked member") — factually wrong for report-source hiding (the viewer never blocked them). Either accept and document the label for both sources, or specify a source-neutral label (e.g., '숨긴 멤버'). — Severity: minor

## Chain-of-Verification Pass

Second-look findings: D10 (undefined R-15) and D9 (R-11 mis-citation) were discovered only on the re-read — the first pass had accepted risk-ID references without walking the risk table end-to-end. D14 (mask-label semantics under union) emerged from the cross-requirement contradiction scan on second pass. Re-verified:
- Every REQ entry re-read individually (spec.md:L59–L131): no additional pattern violations beyond D5/D6/D7.
- REQ sequencing re-checked end-to-end including plan.md §2 and spec-compact.md: 22/22 consistent, no drift in type tags or substance (spec-compact omits only the [EXISTING] no-change delta row — acceptable).
- Traceability re-walked REQ-by-REQ (all 22, not sampled): complete; D8 is the only REQ↔AC content mismatch.
- Exclusions re-checked for specificity: 13 concrete entries, none vague — PASS.
- Contradiction scan across documents: REQ-RPT-002 "즉시 숨긴다" vs REQ-FLT-003 masking-not-hiding for expense is resolved by surface-specific precedence (union feeds all surfaces; FLT-003 governs the expense surface) — not counted as a defect, but D3 (settlement_request) and D14 (label) fall out of exactly this union design and ARE counted.
- Cross-SPEC contract re-verified: ownership=ACCOUNT-001, SAFETY-first, no-op guard all present and internally aligned across spec.md L22/L51/L164, plan.md §3.5/§10-3, acceptance.md L116/L139 (modulo D13 terminology drift).

## Regression Check (Iteration 2+ only)

N/A — iteration 1, no prior report.

## Recommendation

FAIL — fix instructions for manager-spec, in priority order:

1. (D1, blocking) plan.md:L50 — In REQ-FLT-006's plan text, replace "`getHiddenUserIds` 역조회를 차감한다" with "`getBlockersOf([senderId])`가 반환한 blocker 집합에 속한 수신자를 차감한다" so plan §2 agrees with plan §3.2 (L121/L139), §7 R-14 (L262), §8 (L269), spec.md:L32, and AC-FLT-6.
2. (D2, blocking) plan.md:L135 — Correct "`getBlockersOf(recipientUserIds)`" to "`getBlockersOf([senderId])` 결과를 recipient 집합과 대조해 차감" (match L139).
3. (D3, blocking) Specify the settlement_request display surface: either extend REQ-FLT-003 (and AC-FLT-3) to state that a hidden user's settlement_request rows in expenses-view follow the same retain+mask (or exclude) rule, or add an explicit Exclusions entry stating settlement_request display is not filtered (with rationale). Silence is not an option for a reportable content type given REQ-RPT-002's hiding promise.
4. (D4, blocking) Pin the report entry-point scope: either add the poll/expense/settlement report UI entry points to plan M4 + Delta Markers, or add an Exclusions entry "poll/expense/settlement_request 신고 진입점 UI — v1은 채팅 말풍선만, 백엔드는 4종 수용(추후 확장)". Remove the ambiguous "우선".
5. (D8) spec.md:L117 — Add push to REQ-CPL-002's no-import list to match AC-CPL-2 and the quality gate.
6. (D9, D10) acceptance.md:L116 — cite R-10 only (drop R-11); plan.md — either add an R-15 row to the §7 risk table (account↔safety 순환 의존 — prisma 직접 접근으로 회피) or renumber the three R-15 citations.
7. (D5, D6, D7, D11) Formality sweep: add "(shall)" to REQ-RPT-004; retag REQ-BLK-004 as [Ubiquitous] (or rephrase as a true feature-conditional); soften spec.md:L55's "단일 행위" claim (e.g., "단일 응집 행위 묶음") or split REQ-RPT-003/REQ-BLK-001; reconcile plan.md:L25's "(모듈당 ≤5)" with module 3's 6 REQs (raise the cap wording or restate as module-count limit like spec.md:L55).
8. (D12) Append the 4 plan-gate confirmed decisions to interview.md (or a decision log) with date, so HISTORY's "사용자 확정 결정 4건" claim is verifiable on disk.
9. (D13, D14) Align guard terminology (no-op 가드 vs 후속 태스크) between §3.5 and §10-3; decide and document the mask label for report-only hiding.

Must-pass criteria all hold (MP-1/2/3 PASS, MP-4 N/A) — the FAIL is driven by internal-consistency contradictions on the REQ-FLT-006 push mechanism (the exact cross-SPEC-sensitive path) and two scope/coverage holes. All four blocking items are localized text fixes; no architectural rework is required. Expected to clear in iteration 2.
