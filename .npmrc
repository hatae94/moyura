# Metro (React Native / Expo) cannot reliably resolve pnpm's default symlinked
# node_modules layout. Use a hoisted (flat) node_modules so Metro and Nest/Next
# resolve dependencies the same way npm/yarn would.
node-linker=hoisted
