# SPEC-SAFETY-001 Progress

- Started: 2026-07-02
- Harness level: thorough (priority=critical, security keywords)
- Development mode: tdd
- Scale mode: Full Pipeline (>=10 files, 3 domains: backend/db/web)
- Execution mode: sub-agent + Workflow orchestration (ultracode), current session
- Plan phase: complete (plan-auditor PASS iteration 2, commit 2981bbb)
- Phase 1+1.5 complete: strategy ready, 10 tasks, coverage verified
- T-001 complete: Block+Report models + additive migration (20260702100000_add_safety) applied via diff→db execute→migrate resolve (drift workaround); RLS default-deny both tables, content_type CHECK(4종), report.moim_id FK CASCADE verified via pg; schema.md/rls-policies.md documented; nx lint+build+test(523) backend green.
- T-002 complete: SafetyService 조회 계약 2종 TDD(RED 9-test fail→GREEN→REFACTOR none) — getHiddenUserIds(block∪report union+dedup+인가격리, Promise.all 2쿼리) / getBlockersOf(block-only 역방향, empty-in guard, report 미조회); @MX:ANCHOR 2종(+REASON); safety.service.ts 100% stmt/func/line(branch 80%=Injectable 데코레이터 phantom), nx lint backend clean, full suite 532 green.
- T-003 complete: SafetyService 변이 4종 TDD(RED 25-test fail→GREEN→REFACTOR none) — createReport(content_type 화이트리스트 4종/빈 reason 400 + reporterId=sub 강제(body 불신) + block 미생성) / createBlock(자기 차단 400 + P2002 멱등 findUniqueOrThrow 흡수, invite 선례) / unblock(deleteMany 멱등 + report 불변 + blockerId=sub 격리) / listBlocks(block 행만, report 미조회); CreateReportDto·CreateBlockDto 신규(reporterId/blockerId 필드 없음=mass-assignment 차단); safety.service.ts 100% stmt/func/line·90.9% branch(85%+ 초과), DTO는 로직 없는 선언 파일(기존 poll/notification DTO 컨벤션 동일), nx lint backend clean, full suite 557 green.
- T-004 complete: SafetyController+SafetyModule+app.module 배선 TDD(RED 15-test 404 fail→GREEN→REFACTOR +2 branch test) — 4 라우트(POST /reports 201, POST /blocks 201, DELETE /blocks/:blockedUserId 204, GET /blocks 200) per-route @UseGuards(SupabaseAuthGuard), WHERE 내장 인가(reporterId/blockerId=검증 sub, 위조 body 필드 무시); notification.controller.spec 패턴 통합 테스트(실제 가드 배선 401 + fake Prisma Promise.resolve/reject + P2002 멱등 흡수); SafetyModule(AuthModule import, SafetyService exports=소비 도메인 주입점), app.module SafetyModule 등록(PushModule 뒤); safety-response.dto.ts 신규(Report/Block createdAt→ISO); safety 모듈 100% stmt/func/line·80.76% branch(데코레이터 metadata phantom, 라인 85%+ 충족), tsc 0 error, nx lint backend clean, safety suite 51 green.
- T-005 complete: 읽기 필터 3곳 SafetyService.getHiddenUserIds 주입 TDD(RED→GREEN 서비스별) — chat getHistory senderId notIn(WHERE-level=DB over-fetch 후 take, app trim 불필요 R-1, 페이지 크기 보존 + <limit·nextCursor null) / poll listPolls createdBy notIn(aggregatePolls 표 집계 불변=숨김 대상 표 유지, AC-FLT-2) / notification listForRecipient actorId notIn(nullable notIn=NULL 통과로 시스템/무행위자 알림 보존, AC-FLT-5); 3 도메인 각 생성자 마지막 인자 safety 추가 + *.module.ts SafetyModule import(chat/poll/notification→safety 단방향, safety↛도메인 grep 0 확인); notIn 빈 배열=Prisma no-op 케이스 커버; 통합/컨트롤러/리스너 spec fake Prisma에 block/report 빈 findMany + notIn fake 추가(DI 배선 복구); @MX:NOTE 필터 사이트 3곳(REQ ref); 백엔드 full suite 584 green(+11 신규), tsc 0 error, nx lint backend clean.
- T-006 complete: 지출 표시 목록 작성자 마스킹 TDD(RED 2-fail→GREEN→REFACTOR 테스트 중복 정리) — listExpenses에 getHiddenUserIds 1회 조회 후 maskHiddenCreators 순수 헬퍼로 createdBy∈hiddenIds인 expense를 '차단한 멤버'(BLOCKED_MEMBER_LABEL) 마스킹(map+스프레드=원본 불변, 행 유지). 계산(balance/transactions/total)은 전체 원본 기준으로 마스킹 전 수행=원장 정합(R-2, @MX:WARN 계산/표시 분리 지점 +REASON). ExpenseModule에 SafetyModule import(expense→safety 단방향, grep 0 확인). 선행 확인 결과: listExpenses 반환 shape는 settlement_request 행을 포함하지 않음(SettlementRequest=requestSettlement create 전용, 읽기 경로 부재) → 요청자 마스킹은 vacuous(마스킹 대상 없음), 명시 고정 테스트 추가. 응답에 settlement_request 행 추가는 스코프 확장(DTO/openapi/web)이라 미구현. 신규 5테스트(행 유지+마스킹/계산 불변/∑정합/무차단 passthrough/settlement_request 부재 고정); expense.service.ts 94.31% stmt·95% func(마스킹 경로 100%, 미커버는 기존 computeShares 분기), 백엔드 full suite 589 green(+5), tsc 0 error, nx lint backend clean.
- T-007 complete: 일정 슬롯 뷰어 필터 TDD(RED 1-fail→GREEN→REFACTOR none) — getSchedule은 include:{slots:true} 중첩 로드라 top-level where 대신 응답 매핑 시점에 event.slots를 필터. findUnique 후 event null이면 숨김 조회 생략 후 null 반환(불필요 왕복 회피), 존재하면 getHiddenUserIds(sub) 1회 조회→Set 룩업으로 slot.userId∈hidden 제외(hidden 빈 배열=early return no-op). dates/window 등 이벤트 메타는 협업 편집 작성자 추적 부재로 미필터(한계 — 원본 불변, AC-FLT-4). ScheduleService 생성자 마지막 인자 safety 추가 + ScheduleModule에 SafetyModule import(schedule→safety 단방향, @MX:NOTE 생성자/모듈). 신규 4테스트(hidden 슬롯 제외+타 멤버 유지·요청당 1회 호출/dates·window 원본 불변/hidden 빈 no-op 전체 통과/미설정 null+숨김 미조회); schedule.service.spec 41 green, nx lint backend clean.
- T-009 complete: 웹 UI(테스트 하네스 부재 — build/lint/typecheck 검증) — lib/safety/{api.ts,actions.ts} 신규(폴즈/멤버스 헬퍼 미러, /reports·/blocks·DELETE /blocks/:id·GET /blocks를 path-as-never로 호출 = schema 재생성 비의존). 채팅 말풍선 신고 진입(Siren 버튼, own 제외) → 사유 폼 → createReport(report만) → applyHiddenAndReload(hidden 추가+setMessages([])+재조회, 서버 union 필터 반영) → "차단할까요?" prompt → 수락 시에만 createBlock(REQ-RPT-003, 거부는 report 숨김만 유지). 실시간 handleIncoming + 미지-발신자 재조회 경로 둘 다 hiddenRef.current.has(senderId) 드롭(hidden = GET /blocks 시딩 + 이번-세션 신고/차단분; report union 엔드포인트 부재라 이전-세션 신고분 실시간은 서버 히스토리 필터가 재조회로 커버 — 코드주석 명시). 멤버 목록 차단 버튼(Ban, showBlock=본인 제외 전 멤버, owner showControls와 독립) + 확인 다이얼로그 + blockAction(revalidatePath). 프로필 blocked-members-section(신규 client) + page.tsx GET /blocks 조회(실패=빈 폴백) + unblockAction. member-actions.ts는 "use server" 재-export가 모듈 무효화 → 제거하고 members-section이 @/lib/safety/actions에서 blockAction 직접 import. nx lint web 0 error, nx typecheck web 0 error(openapi/api-client 의존 재생성 동반), nx run web:build 0 error(14 라우트 정상).
- T-008 complete: 발신 역방향 필터(REQ-FLT-006) TDD(RED 1-fail→GREEN→REFACTOR none) — push.listener handleChatMessageCreated에서 recipientUserIds(moim_member−sender) 산정 직후 excludeBlockersOfSender 헬퍼로 getBlockersOf([senderId]) blocker에 속한 recipient 차감(A→B block → B 발신 시 A FCM 미발신, 잠금화면 미도달). block만 억제하며 report는 미포함(getBlockersOf=block.findMany만, 읽기 경로 getHiddenUserIds와 의도적 다른 소스). 이중 best-effort: 수신 대상 0명이면 safety 왕복 생략(불필요 조회 회피), getBlockersOf throw는 내부 try/catch로 격리(fail-open=차감 스킵하되 발송은 계속, safety 장애가 채팅 push 전면 중단으로 번지지 않음). PushModule에 SafetyModule import(push→safety 단방향). PushListener 생성자 마지막 인자 safety 추가 + @MX:NOTE(REQ-FLT-006 발신 필터 의도, block만/report 미포함). 신규 4테스트(block 차감+getBlockersOf 1회 호출 검증/report-only 대조 push 유지/getBlockersOf throw fail-open 발송 유지/수신 대상 0명 safety 미조회); push.listener.ts 100% stmt/func/line·80% branch(데코레이터 phantom 24-40), push suite 58 green, 백엔드 full suite 597 green(+4), tsc 0 error, nx lint backend clean.
- T-010 complete: 계약 재생성 + 정적 검사 + 운영 문서 + 위임(코드 로직 없음 — 검증/문서 태스크). openapi.json 재생성(nx run backend:openapi = build→node dist/src/openapi.js) + api-client schema.d.ts 재생성(nx run api-client:generate = openapi-typescript) — safety 3라우트(/reports·/blocks·/blocks/{blockedUserId}) 양 산출물에 반영 확인. M5-3 비순환 grep: safety/**의 chat/poll/expense/schedule/notification/push import 0건(safety는 @nestjs·generated/prisma·prisma·auth·자체 dto만 import); push→safety 단방향 확인(push.module→SafetyModule, push.listener→SafetyService). M5-4/M5-7 운영 문서 신규 `.moai/project/safety-operations.md`(신고 report 필드별 수동 DB 조회 SQL + 24h 조치 절차 + per-viewer/관리자UI 부재 한계 + §5 고아 정리 위임 통지: SAFETY 테이블 병합 완료→ACCOUNT-001 deleteAccount no-op 가드 해제·block/report deleteMany 2줄 활성화, Prisma 직접 접근 R-15 순환 회피, 양쪽 plan 정합 확인). 최종 게이트: backend typecheck 0 error, api-client typecheck 0 error, nx lint backend clean, safety 모듈 커버리지 100% stmt/func/line·80.76% branch(데코레이터 emitDecoratorMetadata phantom = 프로젝트 공통 게이트 흡수, MOIM-001/CHAT-002 선례 — 라인 100%로 85%+ 충족), 백엔드 full suite 597 green(신규 로직 없음 무회귀).
